# Module `config`

*Fichier source : `config.py`*

## Fonctions
- [`__new__(cls)`](#__new__)
- [`_load_config(cls)`](#_load_config)

---

## `__new__(cls)` { #__new__ }

```python
def __new__(cls)
```

Pas de docstring

---

## `_load_config(cls)` { #_load_config }

```python
def _load_config(cls)
```

Charge la configuration depuis le fichier JSON.

---

